[project]
name = "chattie-py"
version = "0.1.0"
description = "Python port of Chattie LINE English grading bot"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
chattie-py = "server:main"

[tool.pytest.ini_options]
pythonpath = ["."]
